doctype html

html
    head
        meta(charset='utf-8')
        title= 'Pug Page'
    body
        include head.pug
        h1(style={color:'red', background:'green'})= 'H1 title'
        hr
        ul#menu.menuClass
            each n in numbers
                li=n
        script.
            let num = 8;
            if (num < 10)
                console.log(`${num} < 10`)
            else 
                console.log(`${num} > 10`)

        - let count = 0;
        ul 
            while count < 13
                li= count++

        include footer.pug
            